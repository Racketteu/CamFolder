<Window x:Class="CamFolder.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:views="clr-namespace:CamFolder.View"
        xmlns:viewModels="clr-namespace:CamFolder.ViewModel"
        Title="Cam Folder" Height="375" MinHeight="375" Width="650" MinWidth="650"
        Style="{StaticResource MaterialDesignWindow}">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModels:HomeViewModel}">
            <views:HomeView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:MoveFileViewModel}">
            <views:MoveFileView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:RenameFileViewModel}">
            <views:RenameFileView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:SortViewModel}">
            <views:SortView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:SettingsViewModel}">
            <views:SettingsView />
        </DataTemplate>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="*" MaxHeight="50"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="2">
            <ItemsControl ItemsSource="{Binding MenuButton}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding Command}" 
                                Height="50" 
                                materialDesign:ButtonAssist.CornerRadius="7"
                                Style="{StaticResource MaterialDesignFlatSecondaryButton}"
                                Margin="2">
                            <StackPanel Orientation="Vertical" 
                                        VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="{Binding Icon}" 
                                                         HorizontalAlignment="Center" 
                                                         Width="24" 
                                                         Height="24"/>
                                <TextBlock Text="{Binding Title}" 
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <materialDesign:PackIcon 
                Grid.Row="0"
                Grid.RowSpan="2"
                Grid.Column="0"
                Kind="User" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center" 
                Width="24"
                Height="24"/>
            <TextBlock 
                Grid.Column="1"
                Text="USERNAME"
                VerticalAlignment="Center"/>
            <TextBlock 
                Grid.Column="1"
                Grid.Row="1"
                Text="31/08/2025" 
                VerticalAlignment="Center"/>
        </Grid>

        <Grid Grid.Column="1"
              Grid.RowSpan="2">
            <ContentControl Content="{Binding CurrentViewModel}" />
        </Grid>
    </Grid>
</Window>
